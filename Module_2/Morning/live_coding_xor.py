# Live coding XOR cipher
# One ciphertext can be the result of two different plaintexts, 
# if the encryption was done using two different keys.

p1 = b"ATTACK"
p2 = b"DEFEND"



# Convert to binary
"""p1bin = bytearray()
p2bin = bytearray()
for c1 in p1:
    p1bin.append(ord(c1))

for c2 in p2:
    p2bin.append(ord(c2))

print(p1bin)
print(p2bin)"""
